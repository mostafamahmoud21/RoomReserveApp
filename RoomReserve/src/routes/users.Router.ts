// import express from 'express';
// import { getUsers, deleteUserById, addUser, updateUser} from '../controllers/users.controller';

// export const userRouter = express()

// userRouter.route('/').get(getUsers).post(addUser)
// userRouter.route('/:id').delete(deleteUserById).put(updateUser)